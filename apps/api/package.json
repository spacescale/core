{
  "name": "@spacescale/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go build -v ./...",
    "dev": "go run .",
    "test": "go test ./... -race -cover",
    "clean": "rm -f coverage.out coverage.html"
  }
}
