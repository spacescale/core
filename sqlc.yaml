version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "apps/db/migrations/*.sql"
    queries:
      - "apps/api-control-plane/internal/postgres/queries/*.sql"
    gen:
      go:
        package: "postgres"
        out: "apps/api-control-plane/internal/postgres/gen"
        sql_package: "pgx/v5"
        overrides:
          - db_type: "uuid"
            nullable: false
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "uuid"
            nullable: true
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
              pointer: true
          - db_type: "timestamptz"
            nullable: false
            go_type:
              import: "time"
              type: "Time"
          - db_type: "timestamptz"
            nullable: true
            go_type:
              import: "time"
              type: "Time"
              pointer: true
          - db_type: "text"
            nullable: true
            go_type:
              type: "string"
              pointer: true
          - db_type: "pg_catalog.int4"
            nullable: true
            go_type:
              type: "int32"
              pointer: true
        emit_interface: false # generate go interface for queries good for mocking
        emit_prepared_queries: false # generates prepared statement version of queries
