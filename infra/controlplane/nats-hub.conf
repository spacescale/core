# NATS hub for the control plane.
# Accepts client connections and leaf node connections from compute nodes.
# In production, add a cluster {} block for 3-node RAFT consensus.

listen: 0.0.0.0:4222

jetstream {
  store_dir: /data
}

leafnodes {
  listen: 0.0.0.0:7422
}

http_port: 8222
