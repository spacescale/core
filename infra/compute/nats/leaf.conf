# Local NATS leaf node for compute nodes.
# Accepts local client connections and routes to the regional hub.
# $NATS_HUB_HOST is set at boot (cloud-init in production, env var in dev).

listen: 127.0.0.1:4222

leafnodes {
  remotes [
    { url: "nats-leaf://$NATS_HUB_HOST:7422" }
  ]
}
